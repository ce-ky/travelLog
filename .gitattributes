# Normalise line endings to LF in the repo, so switching between Windows and
# macOS/Linux doesn't produce spurious diffs.
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.ttf binary
*.otf binary
